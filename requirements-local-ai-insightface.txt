# Optional experimental dependencies for the InsightFace People provider.
# Install into .venv-insightface so face recognition can stay optional and
# swappable for future commercial-safe providers.
numpy>=1.26.0
Pillow>=10.0.0
opencv-python-headless>=4.9.0
insightface==0.7.3
onnxruntime==1.20.1; platform_system != "Windows"
onnxruntime-gpu==1.20.1; platform_system == "Windows"
